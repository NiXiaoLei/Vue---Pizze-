const state={
    isLogin:false
}
const getters={
    isLogin:state=>state.isLogin
}
const actions={
    
}
const mutations={
    
}

export default{
    state,
    getters,
    mutations,
    actions
}